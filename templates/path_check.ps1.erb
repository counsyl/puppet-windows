$curpath = [Environment]::GetEnvironmentVariable("PATH", "<%= @target %>");
<% if @ensure == 'present' -%>
if (! $curpath.Contains("<%= @directory %>")){ exit 1 };
<% else -%>
if ($curpath.Contains("<%= @directory %>")){ exit 1 };
<% end -%>
